# iron CLI configuration for this Linux/CUDA checkout.
verbose = 0

[bench]
runs = 3
warmup_runs = 1

[build]
default_dtypes = ["f32", "f16", "bf16"]

[runner]
binary = "runner/target/debug/iron-cuda-runner"
