[package]
name = "ffai-kernels"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
description = "Rust DSL for writing fast and stable GPU kernels across all platforms — write once, run fast on Apple Silicon, NVIDIA, AMD and Vulkan supporteddevices"
keywords = ["metal", "gpu", "apple-silicon", "cuda", "hip", "amd", "vulkan", "dsl", "shader"]
categories = ["compilers", "hardware-support"]
publish = false

[lints]
workspace = true

[features]
# CUDA / NVIDIA device runtime (`CudaDevice` and friends). Forwards to
# ffai-kernels-runtime/cuda so downstream ffaik projects only name the facade.
cuda = ["ffai-kernels-runtime/cuda"]
# HIP / ROCm device runtime (`HipDevice` and friends). Forwards to
# ffai-kernels-runtime/hip so downstream ffaik projects only name the facade.
hip = ["ffai-kernels-runtime/hip"]
# Vulkan device runtime (`VulkanDevice` and friends). Forwards to
# ffai-kernels-runtime/vulkan so downstream ffaik projects only name the facade.
vulkan = ["ffai-kernels-runtime/vulkan"]

[dependencies]
ffai-kernels-core.workspace = true
ffai-kernels-macros.workspace = true
ffai-kernels-codegen.workspace = true
ffai-kernels-runtime.workspace = true
inventory.workspace = true
serde.workspace = true
bytemuck.workspace = true
rayon.workspace = true
regex.workspace = true

[target.'cfg(target_os = "macos")'.dependencies]
objc2.workspace = true
objc2-metal.workspace = true
objc2-foundation.workspace = true

[dev-dependencies]
trybuild.workspace = true
